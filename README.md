copier '.env.example' en '.env' et remplir par les valeurs correctes % sécuriser les données sensibles
docker compose up --build % lancer les containers Docker
docker ps % vois la liste des containers en marche
docker stop <<id container>> % arreter un container
frontend sur localhost:3000 % affichage du frontend
backend sur localhost:8000 % affichage du backend