soft_fail: false

directory: "."

# Exclusions pour éviter les faux positifs
skip_checks: "CKV_AWS_20,CKV_K8S_21"

# Fichiers / dossiers à ignorer
skip_paths: "tests/**,frontend/build/**,docs/**,.venv/**"

# Format de sortie
output_format: "sarif"